// this will be the invidual messages for the message container to render
import React from 'react';
import styled from 'styled-components';

const Message = ({mssg}) => {
    return <div>{mssg}</div>
}

export default Message;
